import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  output: "export",
  basePath: "/IndieAppLandingPageTemplate", // required for GitHub Pages
  assetPrefix: "/IndieAppLandingPageTemplate",
};

export default nextConfig;
