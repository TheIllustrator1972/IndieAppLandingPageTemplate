import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // output: "export",
  basePath: "/IndieAppLandingPageTemplate",
  assetPrefix: "/IndieAppLandingPageTemplate/",
};

export default nextConfig;
